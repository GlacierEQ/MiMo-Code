---
name: helix-pro-code
description: Double Helix Alpha/Omega pro-code engineering. Use for all coding tasks in the GlacierEQ APEX ecosystem. Loads CODER-SKILL laws, Spiral Engine bridge, token-efficient execution, and memory prime workflow.
---

# Helix Pro-Code (Alpha + Omega)

## When to activate

Every coding, infra, memory, or consolidation task in this workspace.

## Boot sequence

1. Read `Pro_Code/CODER-SKILL.md` (Alpha identity)
2. Read `pro-code/KNOWLEDGE.md` (Omega bridge) if workers/Nexus/memory involved
3. Run `sm-ops prime "<task summary>"` or read `~/.supermemory/ops/live-context.md`

## Execution laws (Alpha)

- One Big Push — batch related files, single coherent commit
- Gap analysis before building — audit exists, map missing, prioritize tiers
- Verify carryover — read back after create/push/delete
- Token efficiency — front-load signal, tables over prose, skip filler

## Execution surface (Omega)

- Worker mesh: `pro-code/src/workers.ts`
- Memory: `pro-code/src/memory.ts` + `sm-ops` dual-write
- Nexus: `localhost:8002` health + trigger endpoints

## Memory routing

| Need | Tool |
|------|------|
| Session/episodic | Mem0 via `sm-ops` / unified-memory MCP |
| Knowledge/docs | Supermemory via `sm-ops` / supermemory MCP |
| Legal/evidence vectors | Pinecone via unified-memory router |
| Graph/timeline | Neo4j via unified-memory router |

## Never

- Sprawl new dirs without consolidation plan
- Duplicate memory writes without dedup
- Fragment commits that belong together
- Reload full history when primed context exists