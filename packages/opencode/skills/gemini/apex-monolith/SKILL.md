---
name: apex-monolith
description: Unified APEX expertise — hyper-efficiency, token-savings, memory-connect, sequential-thinking, and universal-connector in single activation. Maximizes token savings with full memory/connector mesh.
metadata:
  priority: 10
  hidden: true
  docs:
    - "https://vercel.com/docs"
  mergeSources:
    - hyper-efficiency-flow
    - token-savings
    - memory-connect
    - unified-memory-connect
    - sequential-thinking
    - universal-connector
    - apex-semantic-router
    - operator-is-the-universe
retrieval:
  aliases:
    - apex unified
    - apex monolith
    - master operator
  intents:
    - maximize tokens
    - optimize memory
    - orchestrate agents
    - run apex flow
---

# APEX Monolith — MASTER OF THE TRADE

> Unified skill combining all APEX capabilities into single powerful activation.
> **Reduces overhead** by merging memory, connectors, and optimization patterns.

## When to Use

- Running parallel diagnostics across multiple layers
- Need memory orchestration without chain hops
- Token optimization with full memory mesh access
- Agent orchestration with connector fan-out

## Complete APEX Coverage

| Domain | Skill | Purpose |
|--------|-------|---------|
| **Optimization** | hyper-efficiency-flow | Parallel execution engine |
| **Savings** | token-savings | Cache-reliability pattern |
| **Memory** | memory-connect + unified-memory | Multi-layer storage |
| **Thinking** | sequential-thinking | Multi-step reasoning |
| **Connectors** | universal-connector | MCP/GitHub/etc hub |

## Parallel Execution Pattern
```ts
// Run all diagnostics concurrently (never sequential)
async function runAll() {
  await Promise.all([
    runDiagnostics(),
    loadConstellation(),
    checkConnectors()
  ])
  // All three complete in parallel = 3× token efficiency
}
```

## Memory Mesh Access
```ts
// Single activation gets all 6 layers:
// mem0, supermemory, pinecone, qdrant, context7, memory_plugin
const layers = ['mem0', 'supermemory', 'pinecone', 'qdrant', 'context7', 'memory_plugin']
const layer = selectLayerByIntent(intent) // 0.75 similarity threshold
```

## Connector Hub
```ts
// Universal connector with MCP scaling
const connectors = {
  filesystem: 'apex-filesystem',
  memory: 'unified-memory',
  cloud: 'gemini:gdrive|dropbox|onedrive',
  dev: 'gemini:github'
}
```

## Token Savings Protocol

1. **Phase 1** — Mount Aspen Grove (memory sync)
2. **Phase 2** — Parallel diagnostics + constellation
3. **Phase 3** — Single manifest state (not scattered logs)
4. **Phase 4** — Verification gate before next phase